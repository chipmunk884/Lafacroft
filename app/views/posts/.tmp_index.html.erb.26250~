<h1>Listing of All Posts</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Date</th>
    <th>Author</th>
    <th>Content</th>
  </tr>

<% @posts.each do |post| %>
  <tr class="<%= cycle('scaffoldodd', 'scaffoldeven') %>">
    <td id="titlepost"><%=h post.title %></td>
    <td id="datepost"><%=h post.date %></td>
    <td id="authorpost"><%=h post.author %></td>
    <td id="contentpost"><%=h post.content %></td>
    <td id="showeditpost"><%= link_to 'Show', [:admin, post] %><br />
		<%= link_to 'Edit', edit_admin_post_path(post) %><br />
		<%= link_to 'Destroy', [:admin, post], :confirm => 'Are you sure?', :method => :delete %>
	</td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New post', new_admin_post_path %>