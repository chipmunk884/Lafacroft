<h1>Lafacroft Posts</h1>
<%= button_to "New Post", new_post_path %><br />
<h2>Home Page Posts</h2>
<ul class="adminposts">
	<% if !(@posts.nil?) %>
	<% @posts.each do |post| %>
	  	<li id="<%= post.id %>">
	  		<%= link_to h(post.title), edit_post_path(post) %> - <%= link_to 'Delete', post, :confirm => 'Are you sure?', :method => :delete %><br />
			Posted on <%= post.created_at.strftime('%m/%d/%Y') %> by <%= h post.author %>
		</li>
	<% end %>
	<% end %>
</ul>
<h2>Tutorial Posts</h2>
<ul class="adminposts">
	<% if !(@tutorials.nil?) %>
	<% @tutorials.each do |post| %>
	  	<li id="<%= post.id %>">
	  		<%= link_to h(post.title), edit_post_path(post) %> - <%= link_to 'Delete', post, :confirm => 'Are you sure?', :method => :delete %><br />
			Posted on <%= post.created_at.strftime('%m/%d/%Y') %> by <%= h post.author %>
		</li>
	<% end %>
	<% end %>
</ul>